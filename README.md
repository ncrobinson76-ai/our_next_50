# our_next_50

A minimal "Hello World" web server built with Node.js's built-in `http` module (no external dependencies).

## Run

```bash
npm start
```

The server listens on port 5000 and responds to any request with `Hello World`.
