// Moved to packages/synthesis-core in Package 9, shared with packages/api's
// weekly-level safety gate. Re-exported here so nothing else in this
// package needs to change its import path.
export * from "../synthesis-core/safetyCheck";
